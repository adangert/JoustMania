apiVersion: 1

datasources:
  # VictoriaMetrics (Prometheus-compatible) for high-frequency metrics
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://victoria-metrics:8428
    isDefault: true
    editable: false
    jsonData:
      timeInterval: "10ms"  # Support 100Hz queries
